services:
  bank_1_api:
    container_name: bank_1_api
    image: 'eclipse-temurin:17-jdk'
    working_dir: '/app'
    command: bash -c "./gradlew clean assemble && java -jar ./build/libs/BankAPI-0.0.1-SNAPSHOT.jar"
    ports:
      - '8081:8080'
    depends_on:
      - bank_1_postgres
    volumes:
      - '.:/app'
  bank_1_postgres:
    container_name: bank_1_postgres
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=bank1db'
      - 'POSTGRES_PASSWORD=postgres'
      - 'POSTGRES_USER=postgres'

#  mongodb:
#    image: 'mongo:latest'
#    environment:
#      - 'MONGO_INITDB_DATABASE=bank2db'
#      - 'MONGO_INITDB_ROOT_PASSWORD=mongo'
#      - 'MONGO_INITDB_ROOT_USERNAME=mongo'
#    ports:
#      - '27017:27017'
